#include <iostream>
#include <string>
using namespace std;

int main() {
    string firstInput, secondInput, combined;

    for (int i = 0; i < 3; ++i) {
        cout << "Attempt " << (i + 1) << endl;

        cout << "First string: ";
        getline(cin, firstInput);

        cout << "Second string: ";
        getline(cin, secondInput);

        combined = firstInput + secondInput;

        cout << "Combined string: " << combined << "\n\n";
    }

    return 0;
}
